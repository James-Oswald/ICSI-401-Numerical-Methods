
T = table();
T.Properties.VariableNames{'Var1'}='n';
T.Properties.VariableNames{'Var2'}='I_n(0.5)';
T.Properties.VariableNames{'Var3'}='e^0.5 - ';
for i = 0:15
    T(i + 1,:) = {10^i, I(10^i, 0.5), exp(0.5) - I(10^i, 0.5)};
end
disp(T);
    
function ret = I(n, x)
    ret = (1 + x/n)^n;
end